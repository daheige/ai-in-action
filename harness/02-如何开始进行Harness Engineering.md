要开始进行Harness Engineering，核心是转变角色：从“写代码的人”变为“设计系统的人”。你不需要一开始就构建复杂框架，而是从一个小闭环开始，逐步搭建让AI能自主运行、自我纠正的工程环境 。

---

# 一、第一步：明确你的“骑手”角色
Harness Engineering 的起点，是接受一个事实：你不再亲自编码，而是为AI Agent设计“赛道”。  
就像赛车手不造车也不修路，但要确保车辆在正确的轨道上高速行驶。

> 🎯 关键思维转变：  
> 过去你关心“这行代码怎么写”，  
> 现在你要问：“如何让AI永远不写出坏代码？”

---

# 二、四步落地法：从零启动Harness

1. 定义最小任务边界（Constrain）
选择一个具体、可闭环的小任务，比如“只允许AI修改用户登录模块”。
- 设置物理边界：通过目录级 `.gitignore` 或权限控制，限制Agent只能访问特定文件夹。
- 建立规则清单：如“禁止硬编码密码”、“必须使用加密函数”等，写入 `AGENTS.md` 文档 。

> 🔧 工具建议：用 Linter + Git Hook 实现硬约束，违反即阻断提交。

2. 构建上下文地图（Inform）
不要指望AI“自己看懂项目”，你要为它画一张导航图。
- 在项目根目录创建 `AGENTS.md`，包含：
  - 项目目标
  - 架构概览
  - 关键依赖关系
  - 修改流程说明
- 子模块下设 `src/auth/AGENTS.md`，提供局部上下文 。

> ✅ 最佳实践：文档要像法律条文一样清晰无歧义，AI才能当作执行依据。

3. 建立自动验证闭环（Verify）
这是Harness的“心跳”——让AI犯错成本趋近于零。
- 配置CI/CD流水线，包含：
  - 单元测试
  - 接口兼容性检查
  - 安全扫描
- 当测试失败时，自动触发回滚并通知Agent重新尝试 。

> 🔄 进阶做法：接入Chrome DevTools协议，让AI能自主验证前端修复效果 。

4. 设计反馈与修正机制（Correct）
每一次失败都应成为系统的“免疫记忆”。
- 记录Agent失败案例，生成“防错规则”：
  - 如“当出现空指针异常时，自动添加判空逻辑”
- 部署“Doc Gardening Agent”，定期扫描并更新过时文档 。

> 🧠 类比：就像JVM的垃圾回收（GC），系统应能自我清洁 。

---

# 三、推荐入门路径（适合新手）

| 阶段 | 动作 | 目标 |
|------|------|------|
| 第1周 | 搭建 `AGENTS.md` + Linter 规则 | 让AI“知道不能做什么” |
| 第2周 | 接入自动化测试 | 实现“输出即验证” |
| 第3周 | 引入沙箱环境（Worktree隔离） | 避免污染主代码库  |
| 第4周 | 部署首个全自治任务（如自动生成API文档） | 完成“从Prompt到Merge”闭环 |

---
