Harness Engineering 目标设定模板

1. Constrain（约束）目标
> 定义AI行为边界，防止越界或破坏性操作。

- 架构约束：
  - 禁止修改核心模块（如 `/src/core`）。
  - 仅允许在指定目录（如 `/src/features/login`）进行修改。
- 安全约束：
  - 禁止硬编码任何敏感信息（如API Key、密码）。
  - 禁止调用未授权的外部API。
- 流程约束：
  - 所有修改必须通过CI/CD流水线。
  - 任务完成后自动销毁沙箱环境。

2. Inform（告知）目标
> 提供AI所需完整、结构化的上下文信息。

- 任务目标：
  - 明确业务需求（如“实现用户登录功能”）。
  - 指定优先级和交付标准（如“支持OAuth2.0登录”）。
- 上下文信息：
  - 提供 `AGENTS.md` 文件，包含项目架构、接口规范、依赖关系。
  - 提供结构化文档目录（如 `/docs/specs`, `/docs/arch`）。
- 动态信息：
  - 实时提供状态信息（如当前分支、测试覆盖率）。

3. Verify（验证）目标
> 建立自动化质量检查机制，确保输出符合预期。

- 功能验证：
  - 核心功能响应时间 ≤ 500ms。
  - 所有关键路径通过单元测试。
- 代码质量：
  - 代码风格符合团队规范（通过 ESLint、Prettier）。
  - 无高危安全漏洞（通过 SAST 扫描）。
- 合规验证：
  - 所有修改通过 CI/CD 验证。
  - 代码审查通过后方可合并。

4. Correct（纠正）目标
> 建立自动反馈与修复机制，形成闭环。

- 错误反馈：
  - 测试失败时，返回具体错误日志与修复建议。
- 自动修复：
  - AI根据错误信号重新生成代码，无需人工干预。
- 防错积累：
  - 将常见错误模式转化为规则，防止重复发生。
  - 定期更新“错误模式库”与“修复策略库”。

---

该模板可帮助你为具体项目定义清晰的 Harness Engineering 目标，确保AI在可控、可验证的环境中高效运行。
