设定Harness Engineering的目标，关键在于将模糊的“让AI干活”转化为可执行、可验证、可闭环的工程体系。其核心是围绕 Constrain（约束）、Inform（告知）、Verify（验证）、Correct（纠正） 四个动词构建目标框架，确保AI在明确边界内自主运行，并能自我修正。

---

# 一、目标设定原则：由人定义，由系统执行
Harness Engineering 的起点不是技术实现，而是清晰的目标定义。人类负责“定方向、划红线、设标准”，AI负责“走路径、试方案、调细节” 。  
目标必须满足：具体、可观测、可验证、可自动化反馈。

> 🎯 原则示例：  
> ❌ 模糊目标：“提升代码质量”  
> ✅ 工程化目标：“所有AI生成代码必须通过单元测试，且无高危安全漏洞”

---

# 二、四大动作的目标设定方法

1. Constrain（约束）：定义“不能做什么”
目标是划定AI的行为边界，防止越界、幻觉或破坏性操作。

- 目标设定方向：
  - 架构层面：禁止反向依赖（如UI层不能调用Service层）
  - 安全层面：禁止硬编码密钥、禁止调用未授权API
  - 工程层面：仅允许在指定目录修改文件，任务完成后自动销毁沙箱环境 

- 落地形式：
  - 通过linter、CI规则、Docker沙箱等机械式强制手段实现 
  - 编写 `AGENTS.md` 中的“禁止清单”，作为AI的硬性守则

> 🔒 关键认知：约束不是靠提示词，而是靠系统机制“物理阻断”

2. Inform（告知）：定义“应该做什么”
目标是为AI提供完整、结构化的上下文，解决“信息缺失”问题 。

- 目标设定方向：
  - 任务目标：明确业务问题、用户群体、优先级功能
  - 上下文地图：提供 `AGENTS.md`、docs/目录、可观测性查询接口（如LogQ）
  - 渐进式披露：不塞万行日志，而是提供“导航图”让AI按需获取 

- 落地形式：
  - 建立项目级 `AGENTS.md`，包含目标、架构、流程说明
  - 设计结构化文档目录（如 `/docs/specs`, `/docs/arch`）
  - 集成UI能力，让AI能读取界面状态（如Chrome DevTools协议）

> 🗺️ 类比：就像给司机导航仪，不是给一堆地图碎片，而是清晰路线指引

3. Verify（验证）：定义“怎样才算对”
目标是建立自动化的质量检查机制，将主观判断转化为客观标准 。

- 目标设定方向：
  - 功能正确性：核心路径耗时 ≤ 2秒，启动时间 ≤ 800ms
  - 代码质量：100%覆盖关键路径测试，无lint错误
  - 安全合规：通过SAST扫描，无CVE高危漏洞

- 落地形式：
  - 配置CI/CD流水线，集成测试、lint、安全扫描
  - 定义“验收标准”为可执行条件，如“PR必须通过所有检查才能合并” 
  - 使用结构化测试验证架构依赖（如Types→Config→Repo→Service）

> ✅ 核心价值：让AI犯错成本趋近于零，失败自动拦截

4. Correct（纠正）：定义“出错后怎么办”
目标是建立自动反馈与修复机制，形成“错误即学习”的闭环 。

- 目标设定方向：
  - 自动反馈：测试失败时，返回具体错误信息与修复建议
  - 自我修复：AI根据错误信号重新生成代码，无需人工介入
  - 防错积累：将常见错误模式转化为规则，防止重复发生

- 落地形式：
  - 在CI失败时，自动触发AI重新尝试，并附带错误日志
  - 建立“错误模式库”，如“空指针异常 → 自动添加判空”
  - 部署“Doc Gardening Agent”，定期更新过时文档，防止上下文腐化 

> 🔄 闭环逻辑：Inform → Agent执行 → Verify → Correct → 再次执行

---

# 三、目标设定示例：构建用户登录模块

| 动作 | 目标设定 |
|------|--------|
| Constrain | 仅允许修改 `/src/auth` 目录；禁止硬编码密码；必须使用加密函数 |
| Inform | 提供 `AGENTS.md` 包含登录流程、接口规范、安全要求 |
| Verify | 单元测试覆盖率 ≥ 90%；接口响应时间 ≤ 500ms；无安全漏洞 |
| Correct | 测试失败时，自动返回错误日志并触发AI重新生成代码 |

---
